{
  "name": "kin-reviewer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pages:build": "npx @opennextjs/cloudflare@latest build",
    "pages:deploy": "npx wrangler@latest deploy",
    "pages:dev": "npx wrangler@latest dev",
    "deploy": "npm run pages:build && npm run pages:deploy",
    "preview": "npm run pages:build && npm run pages:dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "lucide-react": "^0.312.0",
    "next": "15.1.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
